# metadata
name: test sample primitive
description: description

# filters
resource: repository
where:
  - repository.name == "testName" || repository.id == "30"
  - repository.stargazersCount > 20

# primitive configuration
configuration:
    samplePrimitive:
     maxFilesChanged: 5